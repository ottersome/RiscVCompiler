//Testing Program : WORKING
PROGRAM foo(input, output, error) ;
    //Variable declaration WORKING
   var a, b: integer;
   var a, b: real;
   var stringo : string;

   //Test of function WORKING
   function  simplesum(a: integer) : integer;
      begin
         simplesum := a * b
         // simplesum is the return value
      end;
   function myOwnFunciton(identifico: integer) : integer;
        function myOwnFunciton(identifico: integer) : integer;
            prOceDure nestedProc(identi: integer);//Double nest
                begin
                    //Teting If statements WORKING
                    IF left < right THEN
                        swap(estosa)
                    ELSE
                        swap(contosa)
                    ;
                    //ALSO testing expresion because if statements contain them : WORKING
                    IF left> right THEN BEGIN
                        a:= "This is a list";
                        b:= "oF stuff";
                        END
                    ELSE ;

                    b:="Another";

                    a:="Statement below it";

                    //Testing while loops
                    while swap(number) = 2 DO
                       a:="SimpleStatement";
                    while swap(number) = 2 DO BEGIN
                        IF num1 != num2 THEN
                            a:="SimpleStatement"
                        ELSE;
                        a:="More strings";
                    END
                     


                end;

            begin 
                a := "assignon";
            end;
            begin
            a := "assignon";
    end;

    //Testing Procedure : WORKING
    PROCEDURE testo(size: Integer; VAR arr: ARRAY [1..50] of Integer);
        BEGIN
            a:="Testo";
        END;

   begin  
   //Testing multipl assignments : WORKING
      a := 7; b := 13;
      a := 3 + simplesum(10);   // the result is .
      b := 1 * simplesum(-10);   // the result is . 
      stringo := "Stringo stringo stringito";//THis is a string
   end.   // this is the end of the program
